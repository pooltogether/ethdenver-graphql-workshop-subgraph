{
  "name": "pooltogether-churn",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth https://api.thegraph.com/deploy/",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ asselstine/pooltogether-churn",
    "create-local": "graph create --node http://localhost:8020/ asselstine/pooltogether-churn",
    "remove-local": "graph remove --node http://localhost:8020/ asselstine/pooltogether-churn",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 asselstine/pooltogether-churn"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}
